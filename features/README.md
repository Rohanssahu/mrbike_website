# features

Domain-driven modules, one folder per business feature (e.g.
`features/home`, `features/services`, `features/download-app`), each owning
its own components, hooks, and data. Empty until Phase 2 defines the actual
pages/sections.
